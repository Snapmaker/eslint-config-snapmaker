module.exports = {
    rules: {
        // require all requires be top-level
        // https://eslint.org/docs/rules/global-require
        // should be in es6?
        // TODO: remove this later
        'global-require': 'off',
    }
};
